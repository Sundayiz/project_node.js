let generator = require("generate-password");
function password() {
  let password = generator.generate({
    length: 10,
    numbers: true,
    strict: true,
  });
  console.log(password);
}

password();
